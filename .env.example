POSTGRES_USER=delivery
POSTGRES_PASSWORD=delivery
POSTGRES_DB=delivery
# Localhost port 5433 because 5432 may already be in use
DATABASE_URL=postgres://delivery:delivery@localhost:5433/delivery?sslmode=disable

# Optional server settings
PORT=8080
RATE_PROVIDER=dummy

# Webhook secrets (set non-empty values in your .env)
DUMMY_WEBHOOK_SECRET=
KARRIO_WEBHOOK_SECRET=